#+TITLE: E6's GNU Emacs Config
#+AUTHOR: Elihu SoNy (Ehesh)
#+STARTUP: overview
#+OPTIONS: toc:2
#+PROPERTY: header-args:emacs-lisp :tangle post-init.el :mkdirp yes :results silent

This is a *writing-first scholarly environment*, built on [[https://github.com/jamescherti/minimal-emacs.d][minimal-emacs.d]].

* How this config is structured
:PROPERTIES:
:CUSTOM_ID: structure
:END:

minimal-emacs.d gives us two files we *never edit* — =early-init.el= and
=init.el=. They load our own files in a fixed order:

: early-init.el  ->  pre-early-init.el  ->  post-early-init.el
: init.el        ->  pre-init.el  ->  (packages)  ->  post-init.el

Everything in *this* file tangles into =post-init.el= (the last file loaded),
so it runs after minimal-emacs.d's defaults and package setup are in place.

*The golden rule:* edit only this =Config.org=. Never hand-edit the generated
=.el= files — they are overwritten on every tangle. Saving this file
auto-tangles it (see below).

*Portability:* the =:tangle post-init.el= target above is a *relative* path, so
it always resolves next to this file regardless of how Emacs was launched or
which OS you're on. (Absolute =~/.emacs.d/...= targets are fragile on Windows:
=~= only points here when Emacs is launched without =HOME= set, e.g. from the
Start menu, but not from a shell.)

This is the *boot-first minimal slice*: clean, portable, sane defaults, and a
modern-looking theme placeholder. The full look (nano), packages, meow, Org
tooling, etc. layer on in later passes. The previous large config is preserved
in =Config.org.bak= to salvage from.

* File header
:PROPERTIES:
:CUSTOM_ID: file-header
:END:

Tangled banner so the generated file identifies itself.

#+begin_src emacs-lisp
;;; post-init.el --- E6's config (tangled from Config.org) -*- lexical-binding: t; -*-
;;; Commentary:
;;  DO NOT EDIT.  This file is generated by tangling Config.org.
;;  Edit Config.org instead; saving it re-tangles this file.
;;; Code:
#+end_src

* Auto-tangle on save
:PROPERTIES:
:CUSTOM_ID: auto-tangle
:END:

Re-tangle =Config.org= automatically whenever it is saved. Implemented with a
plain =after-save-hook= (no external package) so it works on a fresh clone
before anything is installed. Matches by filename, so it is independent of how
=~= or =user-emacs-directory= happen to resolve.

#+begin_src emacs-lisp
(defun e6/auto-tangle-config ()
  "Tangle the Emacs `Config.org' when it is saved."
  (when (and buffer-file-name
             (string-equal (file-name-nondirectory buffer-file-name)
                           "Config.org"))
    (let ((org-confirm-babel-evaluate nil)
          (message-log-max nil)
          (inhibit-message t))
      (require 'org)
      (org-babel-tangle))))

(add-hook 'after-save-hook #'e6/auto-tangle-config)
#+end_src

* OS-guard helpers
:PROPERTIES:
:CUSTOM_ID: os-guards
:END:

Convenience constants so platform-specific bits (later: Ghostel, external-open,
himalaya paths) stay in one config across Windows + Linux without breaking
either machine.

#+begin_src emacs-lisp
(defconst e6/windows-p (eq system-type 'windows-nt)
  "Non-nil when running on Windows.")
(defconst e6/linux-p (eq system-type 'gnu/linux)
  "Non-nil when running on Linux.")
(defconst e6/mac-p (eq system-type 'darwin)
  "Non-nil when running on macOS.")
#+end_src

* Persistence modes
:PROPERTIES:
:CUSTOM_ID: persistence
:END:

minimal-emacs.d configures the *variables* for these but leaves enabling the
*modes* to us. Turn on the ones a scholar wants running everywhere. (Their
state files land in this directory and are gitignored.)

#+begin_src emacs-lisp
;; Remember recently opened files.
(add-hook 'after-init-hook #'recentf-mode)
;; Persist minibuffer history between sessions.
(add-hook 'after-init-hook #'savehist-mode)
;; Reopen files at the last cursor position.
(add-hook 'after-init-hook #'save-place-mode)
;; Keep buffers in sync with files changed on disk (e.g. after a git pull).
(add-hook 'after-init-hook #'global-auto-revert-mode)
#+end_src

* Small quality-of-life defaults
:PROPERTIES:
:CUSTOM_ID: qol
:END:

A few unobtrusive niceties on top of minimal-emacs.d. Nothing opinionated yet.

#+begin_src emacs-lisp
;; Show matching parens.
(add-hook 'after-init-hook #'show-paren-mode)
;; Soft-wrap long lines in text/prose buffers (writing-first).
(add-hook 'text-mode-hook #'visual-line-mode)
;; Type over the active region.
(add-hook 'after-init-hook #'delete-selection-mode)
#+end_src

* Discoverability — which-key
:PROPERTIES:
:CUSTOM_ID: which-key
:END:

=which-key= pops up available keybindings after a prefix. Built into Emacs 30+,
so no install needed.

#+begin_src emacs-lisp
(add-hook 'after-init-hook #'which-key-mode)
#+end_src

* Look & feel
:PROPERTIES:
:CUSTOM_ID: look
:END:

This is *Pass 1: the look* — a genuinely modern, calm, typeset-document feel
(nano aesthetic), not "90s Emacs". It layers, so we keep our own keys and
packages:

- *fontaine* — font presets (FiraCode mono + Gentium prose)
- *nano-theme* — the colour palette
- *nano-modeline* — a minimal modeline
- *spacious-padding* — airy internal borders/padding
- *mixed-pitch* — prose in Gentium, code/tables in FiraCode
- *org-modern* + *org-appear* — Org reads like a document

All installs are handled by minimal-emacs.d's =use-package-always-ensure=.

** Fonts — fontaine
:PROPERTIES:
:CUSTOM_ID: fontaine
:END:

fontaine defines named font presets and applies them to the =default=,
=fixed-pitch=, and =variable-pitch= faces. Missing fonts fall back to the Emacs
default automatically (no error), so this is safe on a machine without them.

#+begin_src emacs-lisp
(use-package fontaine
  :if (display-graphic-p)
  :config
  (setq fontaine-presets
        '((default
           :default-family "FiraCode Nerd Font"
           :default-height 120
           :fixed-pitch-family "FiraCode Nerd Font"
           :variable-pitch-family "Gentium Plus"
           :variable-pitch-height 1.15)))
  ;; Persist and restore the last-used preset across sessions.
  (fontaine-mode 1)
  (fontaine-set-preset (or (fontaine-restore-latest-preset) 'default)))
#+end_src

** Theme — nano-theme
:PROPERTIES:
:CUSTOM_ID: theme
:END:

The nano palette. Dark by default; swap =nano-dark= for =nano-light= for the
light variant.

#+begin_src emacs-lisp
(use-package nano-theme
  :config
  (load-theme 'nano-dark :no-confirm))
#+end_src

** Modeline — nano-modeline
:PROPERTIES:
:CUSTOM_ID: modeline
:END:

A minimal modeline. This version installs a modeline *per major mode*, so we
hook the ones we live in. Placed at the bottom (=footer=); switch to
=nano-modeline-header= for the classic nano top bar. Buffers without a hook keep
the default modeline for now (more modes get wired in as those features land).

#+begin_src emacs-lisp
(use-package nano-modeline
  :init
  (setq nano-modeline-position 'nano-modeline-footer)
  :hook
  ((prog-mode . nano-modeline-prog-mode)
   (text-mode . nano-modeline-text-mode)
   (org-mode  . nano-modeline-org-mode)))
#+end_src

** Spacing — spacious-padding
:PROPERTIES:
:CUSTOM_ID: spacing
:END:

Adds breathing room: internal padding and clean window borders. Toggle with
=M-x spacious-padding-mode=.

#+begin_src emacs-lisp
(use-package spacious-padding
  :if (display-graphic-p)
  :config
  (spacious-padding-mode 1))
#+end_src

** Org appearance — mixed-pitch, org-modern, org-appear
:PROPERTIES:
:CUSTOM_ID: org-look
:END:

The single biggest "reads like a document" win: prose in the variable-pitch
font, code/tables/keywords staying monospaced (=mixed-pitch=); modern headings,
tags, blocks and tables (=org-modern=); and emphasis markers (=*bold*=,
=/italic/=) hidden until the cursor is on them (=org-appear=, which needs
=org-hide-emphasis-markers=).

#+begin_src emacs-lisp
;; Prose gets the variable-pitch font; code/tables stay fixed-pitch.
(use-package mixed-pitch
  :hook ((org-mode  . mixed-pitch-mode)
         (text-mode . mixed-pitch-mode)))

;; Modern Org rendering.
(use-package org-modern
  :config
  (with-eval-after-load 'org
    (global-org-modern-mode)))

;; Reveal emphasis markers only around point.
(use-package org-appear
  :hook (org-mode . org-appear-mode))

(with-eval-after-load 'org
  (setq org-hide-emphasis-markers t
        org-pretty-entities t))
#+end_src

** Cleaner modeline names — diminish
:PROPERTIES:
:CUSTOM_ID: diminish
:END:

Hide noisy minor-mode lighters so the modeline stays calm.

#+begin_src emacs-lisp
(use-package diminish
  :config
  (with-eval-after-load 'which-key  (diminish 'which-key-mode))
  (with-eval-after-load 'mixed-pitch (diminish 'mixed-pitch-mode))
  (with-eval-after-load 'org-appear (diminish 'org-appear-mode))
  (diminish 'visual-line-mode)
  (with-eval-after-load 'autorevert (diminish 'auto-revert-mode)))
#+end_src

* Editing core
:PROPERTIES:
:CUSTOM_ID: editing-core
:END:

*Pass 2* — how you actually drive Emacs: [[https://github.com/meow-edit/meow][meow]] modal editing, a leader
key that opens [[info:transient][transient]] menus, Magit, and an "AI pipe" helper. Layout is
tuned for *prose/navigation*, not coding, and stays modal-agnostic so a future
swap (e.g. to meep) is one contained change.

*How meow works (read this):* buffers open in *NORMAL* state, where letter keys
are commands, not text. Press =i= to *insert* (type normally), =ESC= to return
to normal. =SPC= is the *leader* — =SPC= then a letter opens a menu (which-key
shows the options). Escape hatch: =M-x meow-global-mode= toggles the whole thing
off if you ever want plain Emacs.

** Meow
:PROPERTIES:
:CUSTOM_ID: meow
:END:

The salvaged QWERTY layout (Elpaca cruft removed; leader wired further down).
=meow-use-clipboard= ties kill/yank to the system clipboard for cross-machine
copy/paste.

#+begin_src emacs-lisp
(use-package meow
  :demand t
  :init
  (setq meow-use-clipboard t)
  :config
  (defun meow-setup ()
    (setq meow-cheatsheet-layout meow-cheatsheet-layout-qwerty)
    ;; Motion state — special/read-only buffers (Dired, Help, ...)
    (meow-motion-overwrite-define-key
     '("j" . meow-next)
     '("k" . meow-prev)
     '("<escape>" . ignore))
    ;; Normal state — the editing keymap
    (meow-normal-define-key
     '("0" . meow-expand-0) '("9" . meow-expand-9) '("8" . meow-expand-8)
     '("7" . meow-expand-7) '("6" . meow-expand-6) '("5" . meow-expand-5)
     '("4" . meow-expand-4) '("3" . meow-expand-3) '("2" . meow-expand-2)
     '("1" . meow-expand-1) '("-" . negative-argument) '(";" . meow-reverse)
     '("," . meow-inner-of-thing) '("." . meow-bounds-of-thing)
     '("[" . meow-beginning-of-thing) '("]" . meow-end-of-thing)
     '("a" . meow-append) '("A" . meow-open-below)
     '("b" . meow-back-word) '("B" . meow-back-symbol)
     '("c" . meow-change) '("d" . meow-delete) '("D" . meow-backward-delete)
     '("e" . meow-next-word) '("E" . meow-next-symbol)
     '("f" . meow-find) '("g" . meow-cancel) '("G" . meow-grab)
     '("h" . meow-left) '("H" . meow-left-expand)
     '("i" . meow-insert) '("I" . meow-open-above)
     '("j" . meow-next) '("J" . meow-next-expand)
     '("k" . meow-prev) '("K" . meow-prev-expand)
     '("l" . meow-right) '("L" . meow-right-expand)
     '("m" . meow-join) '("n" . meow-search)
     '("o" . meow-block) '("O" . meow-to-block)
     '("p" . meow-yank) '("q" . meow-quit)
     '("r" . meow-replace) '("R" . meow-swap)
     '("s" . meow-kill) '("t" . meow-till) '("u" . meow-undo)
     '("U" . meow-undo-in-selection) '("v" . meow-visit)
     '("w" . meow-next-word) '("W" . meow-next-symbol)
     '("x" . meow-line) '("y" . meow-save) '("z" . meow-pop-selection)
     '("'" . repeat) '("<escape>" . ignore)))
  (meow-setup)
  (meow-global-mode 1))
#+end_src

** Magit
:PROPERTIES:
:CUSTOM_ID: magit
:END:

Git from inside Emacs. =C-x g= (or =SPC g g=) opens the status buffer.

#+begin_src emacs-lisp
(use-package magit
  :bind ("C-x g" . magit-status)
  :commands (magit-status magit-dispatch magit-file-dispatch magit-log-current magit-blame))
#+end_src

** AI pipe helper
:PROPERTIES:
:CUSTOM_ID: ai-pipe
:END:

Send the region (or whole buffer) to an external CLI agent on stdin and get the
result back — a tidy wrapper over =shell-command-on-region= (=M-|=). Set
=e6/ai-cli-command= to your CLI. By default the output opens in a side buffer;
with a prefix arg (=C-u=) it *replaces* the region in place.

#+begin_src emacs-lisp
(defcustom e6/ai-cli-command ""
  "Default external CLI for `e6/pipe-to-ai'. It receives text on stdin.
For example \"llm\" or \"claude -p\". Leave empty to be prompted each time."
  :type 'string
  :group 'e6)

(defun e6/pipe-to-ai (beg end &optional replace)
  "Pipe the region (BEG END), or the whole buffer, to a CLI agent.
Without a prefix arg, show the output in *AI Output*; with a prefix arg
REPLACE the region/buffer in place."
  (interactive
   (if (use-region-p)
       (list (region-beginning) (region-end) current-prefix-arg)
     (list (point-min) (point-max) current-prefix-arg)))
  (let ((cmd (read-shell-command "Pipe to: " e6/ai-cli-command)))
    (if replace
        (shell-command-on-region beg end cmd nil t)
      (shell-command-on-region beg end cmd "*AI Output*")
      (with-current-buffer "*AI Output*"
        (view-mode 1))
      (display-buffer "*AI Output*"))))
#+end_src

** Theme toggle helper
:PROPERTIES:
:CUSTOM_ID: theme-toggle
:END:

Flip between the nano dark and light palettes (used by the toggle menu below).

#+begin_src emacs-lisp
(defun e6/toggle-theme ()
  "Toggle between `nano-dark' and `nano-light'."
  (interactive)
  (if (custom-theme-enabled-p 'nano-dark)
      (progn (disable-theme 'nano-dark) (load-theme 'nano-light :no-confirm))
    (disable-theme 'nano-light)
    (load-theme 'nano-dark :no-confirm)))
#+end_src

** Transient menus
:PROPERTIES:
:CUSTOM_ID: transients
:END:

Curated, grouped menus opened from the leader. These use built-in and Magit
commands for now; they gain =consult=-powered versions in the completion pass.
Add new groups here, then wire a leader key to them in the next block.

#+begin_src emacs-lisp
(require 'transient)

(transient-define-prefix e6/buffers-menu ()
  "Buffers."
  [["Switch"
    ("b" "Switch buffer" switch-to-buffer)
    ("n" "Next"          next-buffer :transient t)
    ("p" "Previous"      previous-buffer :transient t)]
   ["Manage"
    ("k" "Kill"    kill-current-buffer)
    ("s" "Save"    basic-save-buffer)
    ("r" "Revert"  revert-buffer)
    ("R" "Rename"  rename-buffer)]
   ["List"
    ("l" "Ibuffer" ibuffer)
    ("i" "Imenu"   imenu)]])

(transient-define-prefix e6/files-menu ()
  "Files."
  [["Open"
    ("f" "Find file"   find-file)
    ("r" "Recent file" recentf-open-files)
    ("d" "Dired"       dired)]
   ["Save"
    ("s" "Save"        save-buffer)
    ("S" "Save as"     write-file)]])

(transient-define-prefix e6/windows-menu ()
  "Windows."
  [["Split"
    ("s" "Below" split-window-below)
    ("v" "Right" split-window-right)]
   ["Move / Remove"
    ("o" "Other"   other-window :transient t)
    ("d" "Delete"  delete-window)
    ("m" "Maximize" delete-other-windows)
    ("=" "Balance" balance-windows)]])

(transient-define-prefix e6/git-menu ()
  "Git (Magit)."
  [["Magit"
    ("g" "Status"        magit-status)
    ("d" "Dispatch"      magit-dispatch)
    ("F" "File dispatch" magit-file-dispatch)]
   ["Inspect"
    ("l" "Log"   magit-log-current)
    ("b" "Blame" magit-blame)]])

(transient-define-prefix e6/toggle-menu ()
  "Toggles."
  [["Display"
    ("l" "Line numbers"   display-line-numbers-mode)
    ("w" "Soft wrap"      visual-line-mode)
    ("t" "Truncate lines" toggle-truncate-lines)
    ("f" "Fill column"    display-fill-column-indicator-mode)]
   ["Look"
    ("m" "Mixed pitch"    mixed-pitch-mode)
    ("T" "Dark/light"     e6/toggle-theme)]])

(transient-define-prefix e6/help-menu ()
  "Help."
  [["Describe"
    ("f" "Function" describe-function)
    ("v" "Variable" describe-variable)
    ("k" "Key"      describe-key)
    ("m" "Mode"     describe-mode)]
   ["Search"
    ("a" "Apropos"  apropos-command)
    ("i" "Info"     info)]])
#+end_src

** Meow leader bindings
:PROPERTIES:
:CUSTOM_ID: leader
:END:

Wire =SPC + key= to quick commands and the menus above. This must come *after*
the menus are defined so meow can resolve the symbols.

#+begin_src emacs-lisp
(with-eval-after-load 'meow
  (meow-leader-define-key
   ;; Quick access
   '("SPC" . execute-extended-command)
   '("."   . find-file)
   '(","   . switch-to-buffer)
   '("TAB" . comment-line)
   '("u"   . universal-argument)
   '("|"   . e6/pipe-to-ai)
   ;; Menus
   '("b"   . e6/buffers-menu)
   '("f"   . e6/files-menu)
   '("g"   . e6/git-menu)
   '("h"   . e6/help-menu)
   '("t"   . e6/toggle-menu)
   '("w"   . e6/windows-menu)))
#+end_src

* File footer
:PROPERTIES:
:CUSTOM_ID: file-footer
:END:

#+begin_src emacs-lisp
(provide 'post-init)
;;; post-init.el ends here
#+end_src
