#+TITLE: E6's GNU Emacs Config
#+AUTHOR: Elihu SoNy (Ehesh)
#+STARTUP: overview
#+OPTIONS: toc:2
#+PROPERTY: header-args:emacs-lisp :tangle post-init.el :mkdirp yes :results silent

This is a *writing-first scholarly environment*, built on [[https://github.com/jamescherti/minimal-emacs.d][minimal-emacs.d]].

* How this config is structured
:PROPERTIES:
:CUSTOM_ID: structure
:END:

minimal-emacs.d gives us two files we *never edit* — =early-init.el= and
=init.el=. They load our own files in a fixed order:

: early-init.el  ->  pre-early-init.el  ->  post-early-init.el
: init.el        ->  pre-init.el  ->  (packages)  ->  post-init.el

Everything in *this* file tangles into =post-init.el= (the last file loaded),
so it runs after minimal-emacs.d's defaults and package setup are in place.

*The golden rule:* edit only this =Config.org=. Never hand-edit the generated
=.el= files — they are overwritten on every tangle. Saving this file
auto-tangles it (see below).

*Portability:* the =:tangle post-init.el= target above is a *relative* path, so
it always resolves next to this file regardless of how Emacs was launched or
which OS you're on. (Absolute =~/.emacs.d/...= targets are fragile on Windows:
=~= only points here when Emacs is launched without =HOME= set, e.g. from the
Start menu, but not from a shell.)

This is the *boot-first minimal slice*: clean, portable, sane defaults, and a
modern-looking theme placeholder. The full look (nano), packages, meow, Org
tooling, etc. layer on in later passes. The previous large config is preserved
in =Config.org.bak= to salvage from.

* File header
:PROPERTIES:
:CUSTOM_ID: file-header
:END:

Tangled banner so the generated file identifies itself.

#+begin_src emacs-lisp
;;; post-init.el --- E6's config (tangled from Config.org) -*- lexical-binding: t; -*-
;;; Commentary:
;;  DO NOT EDIT.  This file is generated by tangling Config.org.
;;  Edit Config.org instead; saving it re-tangles this file.
;;; Code:
#+end_src

* Auto-tangle on save
:PROPERTIES:
:CUSTOM_ID: auto-tangle
:END:

Re-tangle =Config.org= automatically whenever it is saved. Implemented with a
plain =after-save-hook= (no external package) so it works on a fresh clone
before anything is installed. Matches by filename, so it is independent of how
=~= or =user-emacs-directory= happen to resolve.

#+begin_src emacs-lisp
(defun e6/auto-tangle-config ()
  "Tangle the Emacs `Config.org' when it is saved."
  (when (and buffer-file-name
             (string-equal (file-name-nondirectory buffer-file-name)
                           "Config.org"))
    (let ((org-confirm-babel-evaluate nil)
          (message-log-max nil)
          (inhibit-message t))
      (require 'org)
      (org-babel-tangle))))

(add-hook 'after-save-hook #'e6/auto-tangle-config)
#+end_src

* OS-guard helpers
:PROPERTIES:
:CUSTOM_ID: os-guards
:END:

Convenience constants so platform-specific bits (later: Ghostel, external-open,
himalaya paths) stay in one config across Windows + Linux without breaking
either machine.

#+begin_src emacs-lisp
(defconst e6/windows-p (eq system-type 'windows-nt)
  "Non-nil when running on Windows.")
(defconst e6/linux-p (eq system-type 'gnu/linux)
  "Non-nil when running on Linux.")
(defconst e6/mac-p (eq system-type 'darwin)
  "Non-nil when running on macOS.")
#+end_src

* Persistence modes
:PROPERTIES:
:CUSTOM_ID: persistence
:END:

minimal-emacs.d configures the *variables* for these but leaves enabling the
*modes* to us. Turn on the ones a scholar wants running everywhere. (Their
state files land in this directory and are gitignored.)

#+begin_src emacs-lisp
;; Remember recently opened files.
(add-hook 'after-init-hook #'recentf-mode)
;; Persist minibuffer history between sessions.
(add-hook 'after-init-hook #'savehist-mode)
;; Reopen files at the last cursor position.
(add-hook 'after-init-hook #'save-place-mode)
;; Keep buffers in sync with files changed on disk (e.g. after a git pull).
(add-hook 'after-init-hook #'global-auto-revert-mode)
#+end_src

* Small quality-of-life defaults
:PROPERTIES:
:CUSTOM_ID: qol
:END:

A few unobtrusive niceties on top of minimal-emacs.d. Nothing opinionated yet.

#+begin_src emacs-lisp
;; Show matching parens.
(add-hook 'after-init-hook #'show-paren-mode)
;; Soft-wrap long lines in text/prose buffers (writing-first).
(add-hook 'text-mode-hook #'visual-line-mode)
;; Type over the active region.
(add-hook 'after-init-hook #'delete-selection-mode)
#+end_src

* Discoverability — which-key
:PROPERTIES:
:CUSTOM_ID: which-key
:END:

=which-key= pops up available keybindings after a prefix. Built into Emacs 30+,
so no install needed.

#+begin_src emacs-lisp
(add-hook 'after-init-hook #'which-key-mode)
#+end_src

* Theme placeholder
:PROPERTIES:
:CUSTOM_ID: theme
:END:

A *placeholder* so Emacs doesn't look like 1994 while the real nano look is
built later. =modus-themes= are built-in, modern, and highly legible. Flip to
=modus-operandi= for the light variant. This whole block gets replaced when the
nano layer lands.

#+begin_src emacs-lisp
(when (display-graphic-p)
  (load-theme 'modus-vivendi :no-confirm))
#+end_src

* Fonts (graceful, optional)
:PROPERTIES:
:CUSTOM_ID: fonts
:END:

Set the two intended fonts *only if installed* — =find-font= guards mean a
machine missing a font falls back to the default instead of erroring. Full
=fontaine= setup (presets, mixed-pitch) comes with the look pass.

#+begin_src emacs-lisp
(when (display-graphic-p)
  ;; Monospace: FiraCode Nerd Font.
  (when (find-font (font-spec :name "FiraCode Nerd Font"))
    (set-face-attribute 'default nil :family "FiraCode Nerd Font" :height 120)
    (set-face-attribute 'fixed-pitch nil :family "FiraCode Nerd Font"))
  ;; Variable/prose: Gentium Plus.
  (when (find-font (font-spec :name "Gentium Plus"))
    (set-face-attribute 'variable-pitch nil :family "Gentium Plus" :height 130)))
#+end_src

* File footer
:PROPERTIES:
:CUSTOM_ID: file-footer
:END:

#+begin_src emacs-lisp
(provide 'post-init)
;;; post-init.el ends here
#+end_src
